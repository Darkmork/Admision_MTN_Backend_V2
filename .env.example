# =====================================
# MTN Admission System - Backend
# Environment Variables Template
# =====================================
#
# IMPORTANTE:
# 1. Copia este archivo a .env para desarrollo local
# 2. Reemplaza los valores vacíos con tus credenciales reales
# 3. NUNCA hagas commit de .env (está en .gitignore)
# 4. Para producción, configura estas variables en Railway

# =====================================
# Database Configuration
# =====================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Admisión_MTN_DB
DB_USERNAME=admin
DB_PASSWORD=

# =====================================
# JWT Authentication
# =====================================
JWT_SECRET=
JWT_EXPIRATION_TIME=86400000

# =====================================
# Email Configuration (SMTP)
# =====================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
EMAIL_MOCK_MODE=true

# =====================================
# Application Configuration
# =====================================
NODE_ENV=development
PORT=8080

# =====================================
# Variables de Railway (Referencias)
# =====================================
# En Railway, usa estas referencias:
# DB_HOST=${{Postgres.PGHOST}}
# DB_PORT=${{Postgres.PGPORT}}
# DB_NAME=${{Postgres.PGDATABASE}}
# DB_USERNAME=${{Postgres.PGUSER}}
# DB_PASSWORD=${{Postgres.PGPASSWORD}}
